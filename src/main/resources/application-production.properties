# Production configuration for Render deployment
server.port=${PORT:8080}

# Database configuration using Render environment variables
spring.datasource.url=${DATABASE_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate settings for production
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

# PostgreSQL optimizations
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# File upload configuration for production
file.upload-dir=${UPLOAD_DIR:/tmp/uploads}
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB

# Email configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${EMAIL_USERNAME:zenosvalkyre@gmail.com}
spring.mail.password=${EMAIL_PASSWORD:dnlz dijm uuub xhpw}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Admin email for notifications
app.admin.email=${ADMIN_EMAIL:prasanthdit001@gmail.com}

# JWT configuration
jwt.secret.key=${JWT_SECRET_KEY}

# Security and logging
logging.level.org.springframework.security=WARN
logging.level.org.springframework.web=INFO
logging.level.com.example.myProject=INFO

# CORS configuration for production
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:https://your-frontend-app.vercel.app}

# Health check endpoint
management.endpoints.web.exposure.include=health
management.endpoint.health.show-details=always